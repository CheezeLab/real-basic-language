//변수는 어떤 정보에 이름을 붙여서 저장하고 싶을 때 사용

//단순 선언은 위험
//name = "mike";
//age = 30;

//예약어 사용 금지 (class)
//class = "수업";

//let, const
let var1 = "mike";
var1 = "choi";

const con1 = 1000;

// 자바스크립트에서 변수를 선언할 때는,
// 변하지 않는 값은 const,
// 변할 수 있는 값은 let 으로 선언
// 의도치 않는 동작을 방지
// tip. 모든 변수를 const로 선언하고, 나중에 변경될 여지가 있는 것만 let 으로 변경

// 정리
// 1. 변수는 문자와 숫자, $와 _만 사용
// 2. 첫글자는 숫자가 될 수 없다
// 3. 예약어는 사용할 수 없다
// 4. 가급적 상수는 대문자로 사용
// 5. 변수명은 읽기 쉽고 이해할 수 있게 선언
